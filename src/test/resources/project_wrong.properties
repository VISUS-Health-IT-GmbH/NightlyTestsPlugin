# Example properties file as it would be used by a project itself:
# ===============================================================

# which tests should be handled only when running nightly, separated using ","
plugins.nightlytests.listOfTests=
